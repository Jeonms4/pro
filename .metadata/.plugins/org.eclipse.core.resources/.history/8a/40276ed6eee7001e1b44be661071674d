package sec01;

public class Person {
	void action(Workable workable) {
		// 1) 매개 변수 X, return X
//		workable.work();
		// 2) 매개 변수 O, return X
//		workable.work("과자");
		
		// 3) 매개 변수 X, return O
		String gift = workable.work();
		System.out.println(gift);
		
		int count = 5;
		String gift = Workable.work(count);
		System.out.printf("%s")
	}
}
